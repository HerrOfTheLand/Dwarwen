# Dwarven Dream Machine Spec

## Symbols

* \#     - Number constant

## Instrictions

* `PUSH`   - Push 64 bit number to the stack
* `ADD`    - Pops last two numbers off of stack, adds, pushes result
* `HLT`    - Pops stack and returns result as exit code
